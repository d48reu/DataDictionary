---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - src/mdc_encyclopedia/__init__.py
  - src/mdc_encyclopedia/db.py
  - .gitignore
autonomous: true
requirements:
  - CLI-01
  - INGEST-04

must_haves:
  truths:
    - "Running `pip install .` in the project root installs the mdc-encyclopedia package without errors"
    - "The SQLite database is created with all 5 tables (datasets, columns, enrichments, audit_scores, changes) when init_db is called"
    - "Schema version tracking via PRAGMA user_version is set to 1 after initial creation"
    - "Foreign keys are enforced on every database connection"
  artifacts:
    - path: "pyproject.toml"
      provides: "Hatchling build config with src layout and console_scripts entry point"
      contains: "mdc-encyclopedia = \"mdc_encyclopedia.cli:cli\""
    - path: "src/mdc_encyclopedia/__init__.py"
      provides: "Package marker with version"
      contains: "__version__"
    - path: "src/mdc_encyclopedia/db.py"
      provides: "Database initialization with full schema and version management"
      exports: ["init_db", "CURRENT_SCHEMA_VERSION"]
    - path: ".gitignore"
      provides: "Git ignore rules for Python project"
  key_links:
    - from: "pyproject.toml"
      to: "src/mdc_encyclopedia/"
      via: "Hatchling packages directive"
      pattern: 'packages = \["src/mdc_encyclopedia"\]'
    - from: "src/mdc_encyclopedia/db.py"
      to: "SQLite database file"
      via: "sqlite3.connect + PRAGMA user_version"
      pattern: "PRAGMA user_version"
---

<objective>
Create the installable Python package skeleton with Hatchling build config and the complete SQLite database module with all 5 tables needed by downstream phases.

Purpose: Establishes the package structure that `pip install .` depends on and the normalized database schema that all future phases (ingestion, enrichment, audit, diff) write to.
Output: pyproject.toml, package __init__.py, db.py with full schema, .gitignore
</objective>

<execution_context>
@/home/d48reu/.claude/get-shit-done/workflows/execute-plan.md
@/home/d48reu/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create pyproject.toml and package skeleton</name>
  <files>pyproject.toml, src/mdc_encyclopedia/__init__.py, .gitignore</files>
  <action>
Create `pyproject.toml` at project root with:
- `[build-system]` requiring hatchling, backend = "hatchling.build"
- `[project]` section: name = "mdc-encyclopedia", version = "0.1.0", description = "Miami-Dade County Open Data Encyclopedia", requires-python = ">=3.10", license = "MIT", authors = [{ name = "Diego Abreu" }]
- dependencies: click>=8.0, rich-click>=1.8, rich>=13.0
- `[project.scripts]` entry: mdc-encyclopedia = "mdc_encyclopedia.cli:cli"
- `[tool.hatch.build.targets.wheel]` packages = ["src/mdc_encyclopedia"] (MUST be explicit -- do not rely on auto-detection per Pitfall 1 in research)

Create `src/mdc_encyclopedia/__init__.py` with:
```python
"""MDC Open Data Encyclopedia."""
__version__ = "0.1.0"
```

Create `.gitignore` with standard Python ignores: __pycache__/, *.pyc, *.egg-info/, dist/, build/, .eggs/, *.db (SQLite files), .env, .venv/, venv/, .idea/, .vscode/, *.swp
  </action>
  <verify>
    <automated>python -c "import tomllib; d = tomllib.load(open('pyproject.toml', 'rb')); assert d['project']['name'] == 'mdc-encyclopedia'; assert d['project']['scripts']['mdc-encyclopedia'] == 'mdc_encyclopedia.cli:cli'; assert d['tool']['hatch']['build']['targets']['wheel']['packages'] == ['src/mdc_encyclopedia']; print('pyproject.toml valid')"</automated>
    <manual>Verify pyproject.toml has all sections, __init__.py has version, .gitignore covers *.db</manual>
  </verify>
  <done>pyproject.toml exists with correct Hatchling config, src layout packages directive, and console_scripts entry point. __init__.py has __version__. .gitignore covers Python and SQLite artifacts.</done>
</task>

<task type="auto">
  <name>Task 2: Create SQLite database module with full schema</name>
  <files>src/mdc_encyclopedia/db.py</files>
  <action>
Create `src/mdc_encyclopedia/db.py` implementing:

1. Module-level constant `CURRENT_SCHEMA_VERSION = 1`

2. Module-level constant `SCHEMA_V1` containing CREATE TABLE statements for all 5 tables:
   - `datasets`: id TEXT PRIMARY KEY, source_portal TEXT NOT NULL, source_url TEXT, title TEXT, description TEXT, category TEXT, publisher TEXT, format TEXT, created_at TEXT, updated_at TEXT, row_count INTEGER, tags TEXT, license TEXT, api_endpoint TEXT, bbox TEXT, download_url TEXT, metadata_json TEXT CHECK(json_valid(metadata_json) OR metadata_json IS NULL), pulled_at TEXT NOT NULL DEFAULT (datetime('now'))
   - `columns`: id INTEGER PRIMARY KEY AUTOINCREMENT, dataset_id TEXT NOT NULL REFERENCES datasets(id), name TEXT NOT NULL, data_type TEXT, description TEXT, UNIQUE(dataset_id, name)
   - `enrichments`: id INTEGER PRIMARY KEY AUTOINCREMENT, dataset_id TEXT NOT NULL UNIQUE REFERENCES datasets(id), description TEXT, use_cases TEXT, keywords TEXT, department TEXT, update_freq TEXT, civic_relevance TEXT, prompt_version TEXT, enriched_at TEXT NOT NULL DEFAULT (datetime('now'))
   - `audit_scores`: id INTEGER PRIMARY KEY AUTOINCREMENT, dataset_id TEXT NOT NULL REFERENCES datasets(id), composite_score REAL, staleness REAL, completeness REAL, documentation REAL, audited_at TEXT NOT NULL DEFAULT (datetime('now'))
   - `changes`: id INTEGER PRIMARY KEY AUTOINCREMENT, dataset_id TEXT NOT NULL, change_type TEXT NOT NULL, details TEXT, detected_at TEXT NOT NULL DEFAULT (datetime('now'))

3. Function `init_db(db_path: str) -> bool`:
   - Check if file exists before connecting (to determine if newly created)
   - Connect with sqlite3.connect(db_path)
   - Execute `PRAGMA journal_mode=WAL`
   - Execute `PRAGMA foreign_keys=ON`
   - Read current version via `PRAGMA user_version`
   - If version < 1: executescript(SCHEMA_V1), then set PRAGMA user_version to CURRENT_SCHEMA_VERSION
   - Comment placeholder for future upgrades: `# if version < 2: _upgrade_to_v2(conn)`
   - conn.commit() and conn.close()
   - Return True if newly created, False otherwise

4. Function `get_connection(db_path: str) -> sqlite3.Connection`:
   - Connect, set PRAGMA foreign_keys=ON, set row_factory = sqlite3.Row
   - Return the connection (caller manages lifecycle)

For the json_valid CHECK constraint: wrap the SCHEMA_V1 executescript in a try/except. If it fails due to json_valid not being available, use an alternate SCHEMA_V1 string without the CHECK constraint and retry. This handles Python builds without JSON1 extension (per research Open Question 3).
  </action>
  <verify>
    <automated>python -c "
import sys, os, tempfile
sys.path.insert(0, 'src')
from mdc_encyclopedia.db import init_db, CURRENT_SCHEMA_VERSION, get_connection
with tempfile.TemporaryDirectory() as td:
    db = os.path.join(td, 'test.db')
    assert init_db(db) == True, 'Should return True for new DB'
    assert init_db(db) == False, 'Should return False for existing DB'
    conn = get_connection(db)
    tables = [r[0] for r in conn.execute(\"SELECT name FROM sqlite_master WHERE type='table' ORDER BY name\").fetchall()]
    assert 'datasets' in tables, f'Missing datasets table, got: {tables}'
    assert 'columns' in tables, f'Missing columns table, got: {tables}'
    assert 'enrichments' in tables, f'Missing enrichments table, got: {tables}'
    assert 'audit_scores' in tables, f'Missing audit_scores table, got: {tables}'
    assert 'changes' in tables, f'Missing changes table, got: {tables}'
    ver = conn.execute('PRAGMA user_version').fetchone()[0]
    assert ver == CURRENT_SCHEMA_VERSION, f'Version mismatch: {ver}'
    fk = conn.execute('PRAGMA foreign_keys').fetchone()[0]
    assert fk == 1, 'Foreign keys not enabled'
    conn.close()
    print(f'All checks passed: 5 tables, version={ver}, FK=ON')
"</automated>
  </verify>
  <done>db.py creates all 5 tables (datasets, columns, enrichments, audit_scores, changes) with correct columns, foreign keys, and PRAGMA user_version=1. init_db returns True for new databases, False for existing. get_connection returns a connection with foreign keys enabled and Row factory set.</done>
</task>

</tasks>

<verification>
1. pyproject.toml has Hatchling build backend, src layout packages directive, and mdc-encyclopedia console script
2. __init__.py exists with __version__
3. db.py creates 5 tables with correct schema and manages schema versioning
4. .gitignore covers Python and SQLite artifacts
</verification>

<success_criteria>
- `python -c "import tomllib; ..."` validates pyproject.toml structure
- `python -c "from mdc_encyclopedia.db import init_db; ..."` creates all 5 tables with correct foreign keys and schema version
- All files exist at expected paths
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md`
</output>
