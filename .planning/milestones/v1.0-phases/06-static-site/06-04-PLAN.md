---
phase: 06-static-site
plan: 04
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - src/mdc_encyclopedia/site/templates/changes.html
  - src/mdc_encyclopedia/site/templates/quality.html
  - src/mdc_encyclopedia/site/templates/about.html
  - src/mdc_encyclopedia/site/generator.py
autonomous: true
requirements:
  - SITE-07
  - SITE-08
  - SITE-09

must_haves:
  truths:
    - "'What Changed' page shows diff results from the most recent pull comparison"
    - "Data quality report page shows aggregate stats: percent described, stale count, quality grade distribution"
    - "About/disclaimer page explains methodology and includes disclaimer"
    - "All three pages use MDC branding and responsive layout via base template"
  artifacts:
    - path: "src/mdc_encyclopedia/site/templates/changes.html"
      provides: "What Changed page showing new/removed/schema-changed datasets"
      min_lines: 40
      contains: "change"
    - path: "src/mdc_encyclopedia/site/templates/quality.html"
      provides: "Data quality report page with grade distribution and findings"
      min_lines: 40
      contains: "quality"
    - path: "src/mdc_encyclopedia/site/templates/about.html"
      provides: "About/disclaimer page with methodology"
      min_lines: 30
      contains: "Abreu Data Works"
  key_links:
    - from: "src/mdc_encyclopedia/site/generator.py"
      to: "src/mdc_encyclopedia/site/templates/changes.html"
      via: "generator passes changes data from context to template"
      pattern: "render_changes_page"
    - from: "src/mdc_encyclopedia/site/generator.py"
      to: "src/mdc_encyclopedia/site/templates/quality.html"
      via: "generator passes quality_summary data to template"
      pattern: "render_quality_page"
---

<objective>
Build the three report/informational pages: What Changed, Data Quality, and About/Disclaimer.

Purpose: These pages provide transparency and context. The changes page shows catalog evolution over time. The quality page helps residents judge data reliability. The about page establishes credibility and explains methodology.

Output: Three fully-rendered page templates with real data from the SQLite database.
</objective>

<execution_context>
@/home/d48reu/.claude/get-shit-done/workflows/execute-plan.md
@/home/d48reu/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-static-site/06-RESEARCH.md
@.planning/phases/06-static-site/06-01-SUMMARY.md
@src/mdc_encyclopedia/site/generator.py
@src/mdc_encyclopedia/site/context.py
@src/mdc_encyclopedia/site/templates/base.html
@src/mdc_encyclopedia/site/static/style.css
@src/mdc_encyclopedia/db.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: What Changed page and Data Quality report page</name>
  <files>
    src/mdc_encyclopedia/site/templates/changes.html
    src/mdc_encyclopedia/site/templates/quality.html
    src/mdc_encyclopedia/site/generator.py
  </files>
  <action>
**templates/changes.html**: Replace stub with full What Changed page extending base.html.

Layout:
1. **Page header**: "What Changed" (h1) with subtitle "Changes detected in the most recent catalog pull" and the detected_at timestamp.

2. **Summary cards row**: Three stat cards in a flex row:
   - "New Datasets" count (green accent)
   - "Removed Datasets" count (red accent)
   - "Schema Changes" count (amber accent)

3. **New Datasets section**: If any new datasets (change_type="added"), show a list with:
   - Dataset title (link to /dataset/{slug}/ if slug available)
   - Source portal
   - Category chip
   Guard with `{% if changes.added %}`. Show "No new datasets" if empty.

4. **Removed Datasets section**: If any removed datasets (change_type="removed"), show:
   - Dataset title (no link since page may not exist)
   - Removal details from details JSON if available
   Guard with `{% if changes.removed %}`.

5. **Schema Changes section**: If any schema changes (change_type="columns_added" or "columns_removed"), show:
   - Dataset title (link if available)
   - Change description (e.g., "3 columns added: col1, col2, col3")
   - Details parsed from the changes record
   Guard with `{% if changes.schema %}`.

6. **Empty state**: If no changes at all, show "No changes detected between the most recent pulls. The catalog is stable." message with a muted icon or illustration placeholder.

7. **Historical note**: Small muted text at bottom: "Showing changes from the most recent pull only. Run `mdc-encyclopedia diff --all` for full history."

**templates/quality.html**: Replace stub with full Data Quality report page extending base.html.

Layout:
1. **Page header**: "Data Quality Report" (h1) with subtitle "Quality assessment of {total} Miami-Dade County datasets" and generation date.

2. **Overview stat cards**: Four cards in a flex row:
   - Total Datasets (number)
   - Average Quality Score (e.g., "B+ (78%)")
   - Fully Described (count with % of total)
   - Stale Datasets (count, red accent if > 0)

3. **Grade Distribution**: Visual display of grade distribution. For each grade (A through F):
   - Grade badge + count + percentage
   - Horizontal bar chart (CSS-only, width proportional to count). Use a `<div>` with `width: {{ pct }}%` and colored background matching grade color.
   - Sort by grade order (A first, F last)

4. **Top Findings**: Section listing the most common audit findings (from quality_summary.top_findings). Show finding text with count badge. Top 5-10 findings.

5. **Dimension Breakdown**: Three subsections for freshness, completeness, and documentation:
   - Average score for each dimension
   - Brief explanation of what the dimension measures
   - Count of datasets scoring below threshold in each

6. **Methodology note**: Brief paragraph explaining: "Quality scores combine freshness (50%), completeness (30%), and documentation (20%). Grades: A (90-100), B (80-89), C (70-79), D (60-69), F (below 60)."

Guard all sections with `{% if %}` for missing data. If no audit data exists, show "No quality audit data available. Run `mdc-encyclopedia audit` first." message.

**generator.py**: Update `_render_changes_page` and `_render_quality_page`:
- Changes page: Parse changes data from site_data["changes"]. Group by change_type into added/removed/schema lists. Build summary counts. Pass all to template.
- Quality page: Use site_data["quality_summary"] which should contain grade_distribution (dict of grade->count), top_findings (list of (finding, count) tuples), dimension_averages (dict of dimension->avg_score), total_datasets, avg_score, pct_described, stale_count. Ensure context.py builds this data from audit_scores table queries.
  </action>
  <verify>
    <automated>cd /home/d48reu/DataDictionary && python -c "
from jinja2 import Environment, FileSystemLoader, select_autoescape
tmpl_dir = 'src/mdc_encyclopedia/site/templates'
env = Environment(loader=FileSystemLoader(tmpl_dir), autoescape=select_autoescape(['html']))

# Test changes page
t = env.get_template('changes.html')
changes = {'added': [{'title': 'New DS', 'slug': 'new-ds', 'source_portal': 'Hub', 'category': 'GIS'}], 'removed': [], 'schema': [], 'detected_at': '2026-02-26', 'total_added': 1, 'total_removed': 0, 'total_schema': 0}
html = t.render(changes=changes, generated_at='2026-02-26')
assert 'What Changed' in html
assert 'New DS' in html

# Test quality page
t2 = env.get_template('quality.html')
quality = {'total_datasets': 450, 'avg_score': 78, 'avg_grade': 'B', 'pct_described': 85, 'stale_count': 12, 'grade_distribution': {'A': 100, 'B': 150, 'C': 120, 'D': 50, 'F': 30}, 'top_findings': [('Missing description', 45), ('Stale data', 23)], 'dimension_averages': {'freshness': 0.82, 'completeness': 0.75, 'documentation': 0.68}}
html2 = t2.render(quality=quality, generated_at='2026-02-26')
assert 'Data Quality' in html2
assert '450' in html2

print('Changes and quality templates render correctly')
"</automated>
  </verify>
  <done>What Changed page displays new/removed/schema-changed datasets grouped by type with summary cards. Data Quality page shows grade distribution bars, top findings, dimension breakdowns, and methodology. Both handle empty/missing data gracefully.</done>
</task>

<task type="auto">
  <name>Task 2: About/disclaimer page and final generator wiring</name>
  <files>
    src/mdc_encyclopedia/site/templates/about.html
    src/mdc_encyclopedia/site/generator.py
    src/mdc_encyclopedia/site/context.py
  </files>
  <action>
**templates/about.html**: Replace stub with full About/Disclaimer page extending base.html.

Layout:
1. **Page header**: "About the MDC Open Data Encyclopedia" (h1)

2. **Mission section**: Paragraph explaining the project's purpose: making Miami-Dade County's open data catalog discoverable and understandable by non-technical residents. Mention that every dataset gets AI-generated plain-English descriptions, quality scores, and change tracking.

3. **How It Works section**: Brief explanation of the pipeline:
   - **Data Collection**: Metadata pulled from Miami-Dade County's ArcGIS Hub open data portal
   - **AI Enrichment**: Claude AI generates plain-English descriptions, use cases, keywords, department attribution, and civic relevance scores
   - **Quality Audit**: Automated scoring of freshness, completeness, and documentation quality
   - **Change Detection**: Automated comparison between pulls to track new, removed, and modified datasets
   - **Static Site**: This website generated from the enriched database, updated weekly

4. **Disclaimer section** (prominent, bordered box):
   - "This is NOT an official Miami-Dade County product."
   - "This site is an independent civic technology project. It aggregates and presents publicly available metadata from Miami-Dade County's open data portal."
   - "AI-generated descriptions are produced by Claude (Anthropic) and may contain inaccuracies. Always verify critical information with the original data source."
   - "Quality scores are automated assessments and do not reflect official data quality evaluations."

5. **Credits section**:
   - "Built by Abreu Data Works LLC"
   - "Powered by Claude AI (Anthropic) for dataset enrichment"
   - "Data sourced from Miami-Dade County Open Data (gis-mdc.opendata.arcgis.com)"
   - Link to GitHub repository (placeholder href)

6. **Contact section**: Brief line about feedback/issues being welcome via GitHub Issues.

**generator.py**: Ensure `_render_about_page` passes `generated_at` and any other needed context to the about template. This is mostly a static page but benefits from the generated_at timestamp.

**context.py**: Final review and ensure all data-building functions are complete and robust:
- Verify `build_site_data` assembles all required data: datasets (with columns, related_datasets, audit data), categories, stats, changes, quality_summary.
- Ensure JSON field parsing (tags, keywords, use_cases, findings_json) is done in context.py before passing to templates. Use `json.loads()` with try/except fallback to empty list for malformed JSON.
- Ensure all datasets have slugs computed, with collision detection.
- Ensure `quality_summary` includes grade_distribution, top_findings, dimension_averages, total_datasets, avg_score, pct_described, stale_count.
- Ensure `changes` data is grouped by type (added, removed, schema) with summary counts.
- Ensure `stats` includes total_datasets, pct_described, new_this_week (computed from changes with detected_at in last 7 days).
  </action>
  <verify>
    <automated>cd /home/d48reu/DataDictionary && python -c "
from jinja2 import Environment, FileSystemLoader, select_autoescape
tmpl_dir = 'src/mdc_encyclopedia/site/templates'
env = Environment(loader=FileSystemLoader(tmpl_dir), autoescape=select_autoescape(['html']))
t = env.get_template('about.html')
html = t.render(generated_at='2026-02-26')
assert 'Abreu Data Works' in html
assert 'NOT an official' in html or 'not an official' in html.lower()
assert 'Miami-Dade' in html
print('About template renders correctly')
"</automated>
    <manual>Run full export against real database and verify all pages render without errors</manual>
  </verify>
  <done>About page renders with mission, methodology, prominent disclaimer, and credits. context.py robustly builds all template data with JSON parsing and fallback handling. Generator wires all pages together. Full export pipeline produces complete static site.</done>
</task>

</tasks>

<verification>
1. Running export generates /changes/index.html, /quality/index.html, and /about/index.html
2. Changes page shows grouped changes with summary cards
3. Quality page shows grade distribution as visual bars with correct percentages
4. About page contains disclaimer text "Not an official Miami-Dade County product"
5. About page credits Abreu Data Works LLC
6. All pages inherit base template with nav, footer, and MDC branding
7. All templates handle missing/empty data without crashing
8. context.py correctly parses JSON fields and computes all derived data
</verification>

<success_criteria>
- What Changed page shows diff results grouped by type (new, removed, schema changes)
- Data Quality page shows grade distribution, top findings, and dimension breakdowns
- About page explains methodology with prominent disclaimer
- All three pages are responsive and use MDC branding
- Full export pipeline generates complete static site with all page types
</success_criteria>

<output>
After completion, create `.planning/phases/06-static-site/06-04-SUMMARY.md`
</output>
