---
phase: 12-site-ui-polish-ci-pipeline
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .github/workflows/encyclopedia.yml
autonomous: true
requirements: [MULTI-08]

must_haves:
  truths:
    - "The CI pipeline pulls from all three jurisdictions on weekly schedule without manual intervention"
    - "AI enrichment is skipped on scheduled runs and only runs on manual dispatch when enrich_ai input is true"
    - "If one jurisdiction's pull fails, the pipeline continues with remaining jurisdictions and flags the failure in the workflow summary"
    - "The pipeline builds the site with feeds and exports and deploys to GitHub Pages"
    - "Manual dispatch allows triggering the pipeline with an enrich_ai boolean toggle"
  artifacts:
    - path: ".github/workflows/encyclopedia.yml"
      provides: "Multi-jurisdiction CI pipeline with optional enrichment and weekly schedule"
      contains: "enrich_ai"
  key_links:
    - from: ".github/workflows/encyclopedia.yml"
      to: "mdc-encyclopedia pull"
      via: "CLI command that already loops all jurisdictions"
      pattern: "mdc-encyclopedia pull"
    - from: ".github/workflows/encyclopedia.yml"
      to: "mdc-encyclopedia enrich"
      via: "Conditional enrichment gated on event type and input"
      pattern: "enrich_ai.*true"
---

<objective>
Update the GitHub Actions CI pipeline for multi-jurisdiction data pull with optional AI enrichment and failure reporting.

Purpose: The pipeline must automatically pull from all three jurisdictions, optionally enrich, build the site with all outputs (feeds, exports), and deploy -- without manual intervention on the weekly schedule.
Output: Updated encyclopedia.yml workflow with enrich_ai input, conditional enrichment, and failure summary.
</objective>

<execution_context>
@/home/d48reu/.claude/get-shit-done/workflows/execute-plan.md
@/home/d48reu/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-site-ui-polish-ci-pipeline/12-RESEARCH.md
@.planning/phases/12-site-ui-polish-ci-pipeline/12-CONTEXT.md

@.github/workflows/encyclopedia.yml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update CI workflow for multi-jurisdiction pipeline with optional enrichment</name>
  <files>.github/workflows/encyclopedia.yml</files>
  <action>
Update `.github/workflows/encyclopedia.yml` with the following changes:

1. **Add `enrich_ai` input** to `workflow_dispatch.inputs`:
   ```yaml
   enrich_ai:
     description: 'Run AI enrichment (dataset + field descriptions)'
     type: boolean
     default: false
   ```
   Keep existing `skip_enrichment` and `force_enrich` inputs for backward compatibility.

2. **Update the "Enrich new datasets" step** to be gated on manual dispatch with `enrich_ai: true`:
   - Change the `if` condition to:
     ```yaml
     if: >-
       github.event_name == 'workflow_dispatch' &&
       inputs.enrich_ai == true &&
       inputs.skip_enrichment != true
     ```
   - This ensures enrichment NEVER runs on scheduled cron, only on manual dispatch with explicit opt-in.
   - Keep the existing threshold logic (25-dataset guard with force_enrich override) inside the step.

3. **Add `enrich-fields` command** after the existing `enrich` command inside the enrichment step:
   ```bash
   mdc-encyclopedia enrich-fields --model claude-haiku-4-5-20251001 --resume
   ```
   This adds field-level enrichment alongside dataset enrichment.

4. **Add `--site-url` to the export command** so Atom feed generates correctly in CI:
   - The export step should use `--site-url` when available. Since this is a GitHub Pages deploy, construct it from the repository:
     ```yaml
     run: >-
       mdc-encyclopedia export -o site
       --base-url /${{ github.event.repository.name }}
       --site-url https://${{ github.repository_owner }}.github.io/${{ github.event.repository.name }}
     ```

5. **Add failure summary output** after the pull step to surface per-jurisdiction results:
   - Add a step after "Pull latest data" that pipes pull output to `$GITHUB_STEP_SUMMARY`:
     ```yaml
     - name: Pull latest data
       if: github.event_name != 'push'
       run: |
         mdc-encyclopedia pull 2>&1 | tee /tmp/pull-output.txt
         echo "## Data Pull Summary" >> $GITHUB_STEP_SUMMARY
         tail -20 /tmp/pull-output.txt >> $GITHUB_STEP_SUMMARY
     ```
   The `pull` command already has per-jurisdiction error isolation (try/except per jurisdiction, continues on failure) and prints a summary table. This captures that output into the workflow summary.

6. **Keep cron schedule as-is** (`0 6 * * 1` -- Monday 6:00 UTC). This matches the existing schedule and the CONTEXT.md weekly requirement.

7. **Keep the push trigger** (`push: branches: [master]`) for export+deploy on push. The `github.event_name != 'push'` guards already prevent pull/audit/diff/enrich on push events.

Do NOT change the deployment method (keep actions/deploy-pages@v4). Do NOT change the DB commit pattern. Do NOT change the permissions block.
  </action>
  <verify>
    <automated>cd /home/d48reu/DataDictionary && python -c "
import yaml
with open('.github/workflows/encyclopedia.yml') as f:
    wf = yaml.safe_load(f)
inputs = wf['on']['workflow_dispatch']['inputs']
assert 'enrich_ai' in inputs, 'Missing enrich_ai input'
assert inputs['enrich_ai']['type'] == 'boolean', 'enrich_ai must be boolean'
assert inputs['enrich_ai']['default'] == False, 'enrich_ai must default to false'
schedule = wf['on']['schedule']
assert len(schedule) > 0, 'Missing schedule trigger'
print('PASS: Workflow has enrich_ai input and schedule trigger')
"</automated>
    <manual>Review the workflow file in GitHub to confirm structure is valid YAML and step conditions are correct</manual>
  </verify>
  <done>
    - enrich_ai boolean input added to workflow_dispatch with default false
    - Enrichment step gated on manual dispatch + enrich_ai true (never runs on schedule)
    - enrich-fields command added alongside enrich command
    - Pull output captured in GitHub Step Summary for failure visibility
    - --site-url added to export command for Atom feed URL generation
    - Existing skip_enrichment, force_enrich, and threshold logic preserved
  </done>
</task>

</tasks>

<verification>
1. `python -c "import yaml; wf=yaml.safe_load(open('.github/workflows/encyclopedia.yml')); assert 'enrich_ai' in wf['on']['workflow_dispatch']['inputs']"` -- input exists
2. `grep -c 'enrich_ai' .github/workflows/encyclopedia.yml` -- referenced in conditions
3. `grep 'enrich-fields' .github/workflows/encyclopedia.yml` -- field enrichment command present
4. `grep 'GITHUB_STEP_SUMMARY' .github/workflows/encyclopedia.yml` -- failure summary wired
5. `grep 'site-url' .github/workflows/encyclopedia.yml` -- Atom feed URL configured
</verification>

<success_criteria>
- CI workflow YAML is valid and parseable
- Scheduled runs pull all jurisdictions, skip enrichment, build+deploy site
- Manual dispatch with enrich_ai=true runs both enrich and enrich-fields commands
- Pull failures surface in GitHub workflow summary
- Export includes --site-url for Atom feed generation
</success_criteria>

<output>
After completion, create `.planning/phases/12-site-ui-polish-ci-pipeline/12-02-SUMMARY.md`
</output>
