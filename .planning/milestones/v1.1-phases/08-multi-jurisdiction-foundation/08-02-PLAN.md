---
phase: 08-multi-jurisdiction-foundation
plan: 02
type: execute
wave: 2
depends_on:
  - "08-01"
files_modified:
  - src/mdc_encyclopedia/ingestion/hub_client.py
  - src/mdc_encyclopedia/ingestion/normalizer.py
  - src/mdc_encyclopedia/enrichment/prompts.py
  - src/mdc_encyclopedia/cli.py
autonomous: true
requirements:
  - MULTI-02
  - MULTI-04

must_haves:
  truths:
    - "Hub client accepts a base_url parameter instead of using a hardcoded constant"
    - "Normalizer generates synthetic IDs in format {jurisdiction}_{arcgis_id}"
    - "Normalizer builds source_url and download_url from the registry hub_url, not hardcoded opendata.miamidade.gov"
    - "System prompt includes the jurisdiction display name instead of hardcoded Miami-Dade references"
    - "Running pull --jurisdiction broward pulls only from Broward County"
    - "Running pull with no flag pulls from all registered jurisdictions sequentially"
    - "If one jurisdiction portal fails during pull, the remaining jurisdictions still complete"
    - "Stats command shows a per-jurisdiction dataset count breakdown table"
  artifacts:
    - path: "src/mdc_encyclopedia/ingestion/hub_client.py"
      provides: "Parameterized ArcGIS Hub client"
      contains: "base_url"
    - path: "src/mdc_encyclopedia/ingestion/normalizer.py"
      provides: "Jurisdiction-aware dataset normalization with synthetic IDs"
      contains: "jurisdiction"
    - path: "src/mdc_encyclopedia/enrichment/prompts.py"
      provides: "Template-driven jurisdiction-specific system prompt"
      contains: "jurisdiction_display_name"
    - path: "src/mdc_encyclopedia/cli.py"
      provides: "Multi-jurisdiction pull command and stats breakdown"
      contains: "--jurisdiction"
  key_links:
    - from: "src/mdc_encyclopedia/cli.py"
      to: "src/mdc_encyclopedia/registry.py"
      via: "load_registry() call in pull command"
      pattern: "load_registry"
    - from: "src/mdc_encyclopedia/cli.py"
      to: "src/mdc_encyclopedia/ingestion/hub_client.py"
      via: "create_client(hub_url) per jurisdiction"
      pattern: "create_client.*hub_url"
    - from: "src/mdc_encyclopedia/ingestion/hub_client.py"
      to: "src/mdc_encyclopedia/ingestion/normalizer.py"
      via: "passes jurisdiction and hub_url to normalizer"
      pattern: "normalize_hub_dataset.*jurisdiction"
    - from: "src/mdc_encyclopedia/cli.py"
      to: "src/mdc_encyclopedia/db.py"
      via: "upsert_dataset with synthetic jurisdiction-prefixed ID"
      pattern: "upsert_dataset"
    - from: "src/mdc_encyclopedia/cli.py"
      to: "src/mdc_encyclopedia/db.py"
      via: "GROUP BY jurisdiction query in stats command"
      pattern: "jurisdiction.*COUNT"
---

<objective>
Parameterize the hub client, normalizer, and enrichment prompts for multi-jurisdiction support, then wire the CLI pull command to loop over registered jurisdictions and add per-jurisdiction stats.

Purpose: With the registry and V3 schema in place (Plan 01), this plan makes the full pipeline jurisdiction-aware so users can pull from any registered ArcGIS Hub portal.

Output: Modified hub_client.py, normalizer.py, prompts.py, and cli.py with multi-jurisdiction support.
</objective>

<execution_context>
@/home/d48reu/.claude/get-shit-done/workflows/execute-plan.md
@/home/d48reu/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-multi-jurisdiction-foundation/08-CONTEXT.md
@.planning/phases/08-multi-jurisdiction-foundation/08-RESEARCH.md
@.planning/phases/08-multi-jurisdiction-foundation/08-01-SUMMARY.md
@src/mdc_encyclopedia/ingestion/hub_client.py
@src/mdc_encyclopedia/ingestion/normalizer.py
@src/mdc_encyclopedia/enrichment/prompts.py
@src/mdc_encyclopedia/cli.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Parameterize hub client, normalizer, and enrichment prompts for jurisdiction context</name>
  <files>
    src/mdc_encyclopedia/ingestion/hub_client.py
    src/mdc_encyclopedia/ingestion/normalizer.py
    src/mdc_encyclopedia/enrichment/prompts.py
  </files>
  <action>
    **hub_client.py changes:**
    1. Remove or deprecate the hardcoded `HUB_BASE_URL` constant.
    2. Add a `base_url` parameter to `create_client()` (or the function that initializes the httpx client). The caller will pass the hub_url from the registry.
    3. The `SEARCH_ENDPOINT` path (`/api/search/v1/collections/dataset/items`) remains constant — all ArcGIS Hub portals use the same path.
    4. Ensure `fetch_all_datasets()` (or equivalent pagination function) accepts `base_url` and passes it through to the client.

    **normalizer.py changes:**
    1. Add `jurisdiction: str` and `hub_url: str` parameters to `normalize_hub_dataset()`.
    2. Generate synthetic ID: `id = f"{jurisdiction}_{arcgis_id}"`.
    3. Add `jurisdiction` and `arcgis_id` fields to the returned dict.
    4. Build `source_url` from `hub_url` parameter: `f"{hub_url}/datasets/{arcgis_id}"` — NOT from hardcoded Miami-Dade domain.
    5. Build `download_url` from `hub_url` parameter for Feature Services: `f"{hub_url}/api/download/v1/items/{arcgis_id}/csv?layers=0"`.
    6. Keep all other normalization logic unchanged.

    **prompts.py changes:**
    1. Convert the existing `SYSTEM_PROMPT` string into a `SYSTEM_PROMPT_TEMPLATE` with a `{jurisdiction_display_name}` placeholder where it currently says "Miami-Dade County" in the data librarian role description.
    2. For the department list section: keep the Miami-Dade department list when jurisdiction is Miami-Dade. For other jurisdictions, use a generic approach — omit specific department names and say "the {jurisdiction_display_name} government" (per research recommendation to avoid incorrect department assignments).
    3. Add a `build_system_prompt(jurisdiction_display_name: str) -> str` function that formats the template.
    4. For backward compatibility, keep a `SYSTEM_PROMPT` constant set to `build_system_prompt("Miami-Dade County")` so existing code that references it still works.
  </action>
  <verify>
    <automated>cd /home/d48reu/DataDictionary && python -c "
# Test normalizer
from mdc_encyclopedia.ingestion.normalizer import normalize_hub_dataset
feature = {'id': 'abc123', 'properties': {'title': 'Test', 'description': 'Desc', 'type': 'Feature Service'}}
result = normalize_hub_dataset(feature, jurisdiction='broward', hub_url='https://geohub-bcgis.opendata.arcgis.com')
assert result['id'] == 'broward_abc123', f'Expected broward_abc123, got {result[\"id\"]}'
assert result['jurisdiction'] == 'broward'
assert result['arcgis_id'] == 'abc123'
assert 'geohub-bcgis' in result['source_url'], f'source_url should use broward hub: {result[\"source_url\"]}'
assert 'geohub-bcgis' in (result.get('download_url') or ''), f'download_url should use broward hub'
print('Normalizer OK')

# Test prompts
from mdc_encyclopedia.enrichment.prompts import build_system_prompt, SYSTEM_PROMPT
broward_prompt = build_system_prompt('Broward County')
assert 'Broward County' in broward_prompt, 'Prompt should contain jurisdiction name'
assert 'Miami-Dade' not in broward_prompt, 'Broward prompt should not mention Miami-Dade'
assert 'Miami-Dade' in SYSTEM_PROMPT, 'Default SYSTEM_PROMPT should still be Miami-Dade'
print('Prompts OK')
print('All parameterization verified')
"</automated>
  </verify>
  <done>Hub client accepts base_url parameter. Normalizer generates jurisdiction-prefixed synthetic IDs and builds URLs from registry hub_url. Prompts use jurisdiction display name template. No hardcoded Miami-Dade URLs in normalizer or prompts for non-Miami-Dade jurisdictions.</done>
</task>

<task type="auto">
  <name>Task 2: Wire CLI pull command for multi-jurisdiction loop and add stats breakdown</name>
  <files>src/mdc_encyclopedia/cli.py</files>
  <action>
    **Pull command changes:**
    1. Add `--jurisdiction` / `-j` Click option to the `pull` command. Type: string, default: None (meaning pull all). Help text: "Pull from a specific jurisdiction (e.g., broward). Pulls all if omitted."
    2. At the start of pull, call `load_registry()` from `mdc_encyclopedia.registry`.
    3. If `--jurisdiction` is provided, validate it exists in registry. If not, print available slugs in red and abort.
    4. If `--jurisdiction` is provided, filter targets to just that one. If omitted, targets = all registered jurisdictions.
    5. Loop over targets sequentially. For each jurisdiction:
       - Print header: `"Pulling from {display_name}..."` in bold.
       - Create hub client with that jurisdiction's `hub_url`.
       - Call the existing fetch/normalize/upsert pipeline, passing `jurisdiction` slug and `hub_url` to the normalizer.
       - Track per-jurisdiction results: count of datasets pulled, any errors.
    6. Wrap each jurisdiction's pull in try/except. On failure: log the error, record it in results, print red warning, `continue` to next jurisdiction. Do NOT abort the entire pull.
    7. After the loop, print a summary table showing per-jurisdiction results (jurisdiction name, dataset count or error status).
    8. The existing change detection (snapshot before/after) should still work — it compares dataset IDs which are now jurisdiction-prefixed.

    **Stats command changes:**
    1. Add a "Datasets by Jurisdiction" Rich table to the stats output.
    2. Query: `SELECT jurisdiction, COUNT(*) as cnt FROM datasets GROUP BY jurisdiction ORDER BY jurisdiction`.
    3. Display each jurisdiction's count, then a total row.
    4. Place this section after the existing portal counts section.

    **Import additions:**
    - `from mdc_encyclopedia.registry import load_registry`

    CRITICAL: The normalizer call must pass `jurisdiction=slug` and `hub_url=config['hub_url']` so synthetic IDs are generated correctly.

    CRITICAL: Keep the existing single-jurisdiction pull flow working — if a user has only Miami-Dade in their registry, the command should behave identically to before except with the new ID format.
  </action>
  <verify>
    <automated>cd /home/d48reu/DataDictionary && python -c "
from mdc_encyclopedia.cli import cli
from click.testing import CliRunner
runner = CliRunner()
# Test that --jurisdiction flag exists and validates
result = runner.invoke(cli, ['pull', '--jurisdiction', 'nonexistent', '--help-check'], catch_exceptions=False)
# Just verify the pull command accepts the --jurisdiction flag
result = runner.invoke(cli, ['pull', '--help'])
assert '--jurisdiction' in result.output or '-j' in result.output, 'Missing --jurisdiction flag'
print('CLI --jurisdiction flag exists')
# Test stats command runs
result = runner.invoke(cli, ['stats'])
print(f'Stats exit code: {result.exit_code}')
print('CLI verification complete')
"</automated>
    <manual>Run `mdc-encyclopedia pull --jurisdiction broward` to verify it fetches Broward datasets. Run `mdc-encyclopedia stats` to see jurisdiction breakdown.</manual>
  </verify>
  <done>Pull command supports --jurisdiction flag for single-jurisdiction pulls and loops all jurisdictions when omitted. Failed jurisdictions do not abort the pull. Stats command shows per-jurisdiction dataset count table. Registry is loaded at CLI level and passed through the pipeline.</done>
</task>

</tasks>

<verification>
1. `mdc-encyclopedia pull --help` shows `--jurisdiction` / `-j` option
2. Normalizer produces `broward_abc123` format IDs when jurisdiction='broward'
3. Enrichment prompt for Broward says "Broward County" not "Miami-Dade County"
4. Hub client can be created with any base URL (no hardcoded constant used in fetch path)
5. Stats command shows jurisdiction breakdown when multi-jurisdiction data exists
</verification>

<success_criteria>
- Hub client, normalizer, and prompts are fully parameterized for any jurisdiction
- CLI pull command loops over all registered jurisdictions (or one with --jurisdiction flag)
- Individual jurisdiction failures do not abort the full pull
- Stats command displays per-jurisdiction dataset counts
- No hardcoded Miami-Dade portal URLs remain in the data pipeline
</success_criteria>

<output>
After completion, create `.planning/phases/08-multi-jurisdiction-foundation/08-02-SUMMARY.md`
</output>
