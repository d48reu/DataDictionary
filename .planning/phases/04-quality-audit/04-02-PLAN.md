---
phase: 04-quality-audit
plan: 02
type: execute
wave: 2
depends_on:
  - "04-01"
files_modified:
  - src/mdc_encyclopedia/cli.py
autonomous: true
requirements:
  - CLI-04
  - CLI-06

must_haves:
  truths:
    - "Running `mdc-encyclopedia audit` analyzes all datasets and writes quality scores to the audit_scores table"
    - "Running `mdc-encyclopedia stats` displays dataset count per portal, enrichment status, and quality score distribution"
    - "Audit command shows Rich progress bar during processing and summary table after completion"
    - "Stats command works both before and after audit has been run (graceful empty state)"
    - "Audit command handles zero-dataset database with helpful message"
  artifacts:
    - path: "src/mdc_encyclopedia/cli.py"
      provides: "Wired audit and stats CLI commands"
      contains: "def audit"
  key_links:
    - from: "src/mdc_encyclopedia/cli.py (audit command)"
      to: "src/mdc_encyclopedia/audit/scorer.py"
      via: "import audit_dataset"
      pattern: "from mdc_encyclopedia.audit.scorer import audit_dataset"
    - from: "src/mdc_encyclopedia/cli.py (audit command)"
      to: "src/mdc_encyclopedia/db.py"
      via: "get_all_datasets_for_audit, upsert_audit_score"
      pattern: "upsert_audit_score"
    - from: "src/mdc_encyclopedia/cli.py (stats command)"
      to: "audit_scores table"
      via: "SQL queries for grade distribution"
      pattern: "SELECT letter_grade.*FROM audit_scores"
---

<objective>
Wire the `audit` and `stats` CLI commands into the existing CLI module, connecting them to the scoring engine and database helpers built in Plan 01.

Purpose: These are the user-facing commands that make the quality audit accessible. The `audit` command is the primary deliverable of Phase 4, and `stats` provides the database overview dashboard.

Output: Updated cli.py with working audit and stats commands replacing the stub implementations.
</objective>

<execution_context>
@/home/d48reu/.claude/get-shit-done/workflows/execute-plan.md
@/home/d48reu/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-quality-audit/04-CONTEXT.md
@.planning/phases/04-quality-audit/04-01-SUMMARY.md
@src/mdc_encyclopedia/cli.py
@src/mdc_encyclopedia/db.py
@src/mdc_encyclopedia/audit/scorer.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire audit CLI command</name>
  <files>src/mdc_encyclopedia/cli.py</files>
  <action>
Replace the `audit` stub command in cli.py with a full implementation.

1. Add imports at top of cli.py:
   ```python
   from datetime import datetime, timezone
   import json
   from mdc_encyclopedia.audit.scorer import audit_dataset
   from mdc_encyclopedia.db import get_all_datasets_for_audit, upsert_audit_score
   ```

2. Replace the `audit()` function with:
   - Add `@click.pass_context` decorator so it gets db_path from ctx.obj
   - Get db_path from ctx.obj["db_path"], open connection
   - Call `get_all_datasets_for_audit(conn)` to get all datasets with enrichment and column info
   - If zero datasets: print "[yellow]No datasets found. Run `mdc-encyclopedia pull` first.[/yellow]" and return
   - Set `now = datetime.now(timezone.utc)` at the top (single reference point for all scoring)
   - Rich Progress bar (same spinner/bar/task/elapsed pattern as pull and enrich commands):
     - Iterate over datasets
     - For each: call `audit_dataset(dataset_row, now)`
     - Call `upsert_audit_score(conn, dataset_id, result["composite_score"], result["letter_grade"], result["staleness"], result["completeness"], result["documentation"], json.dumps(result["findings"]))`
     - Advance progress bar
   - After loop: print summary table with Rich Table:
     - Title: "Audit Summary"
     - Rows: Total datasets audited, Grade distribution (count per letter A-F), Top findings (most common findings across all datasets, top 5)
   - Close connection

3. For the grade distribution in the summary, query:
   ```sql
   SELECT letter_grade, COUNT(*) as cnt FROM audit_scores GROUP BY letter_grade ORDER BY letter_grade
   ```
   Display as a Rich Table with Grade, Count, and a simple bar (repeat block char based on count).

4. For the top findings summary, collect findings during the audit loop (accumulate a counter dict of finding strings), then show top 5 most common findings with their counts.
  </action>
  <verify>
    <automated>cd /home/d48reu/DataDictionary && python -c "
import tempfile, os
from mdc_encyclopedia.db import init_db, get_connection, upsert_dataset

# Create temp DB and add a test dataset
p = os.path.join(tempfile.mkdtemp(), 'test.db')
init_db(p)
conn = get_connection(p)
upsert_dataset(conn, {
    'id': 'test-1', 'source_portal': 'arcgis_hub', 'source_url': 'http://test',
    'title': 'Test Dataset', 'description': 'A test dataset with a decent description for scoring purposes.',
    'category': 'Test', 'publisher': 'MDC', 'format': 'CSV',
    'created_at': '2025-01-01T00:00:00+00:00', 'updated_at': '2026-02-01T00:00:00+00:00',
    'row_count': 100, 'tags': '[\"test\"]', 'license': 'Public',
    'api_endpoint': None, 'bbox': None, 'download_url': None, 'metadata_json': None
})
conn.close()

# Run audit via CLI runner
os.environ['MDC_ENCYCLOPEDIA_DB'] = p
from click.testing import CliRunner
from mdc_encyclopedia.cli import cli
runner = CliRunner()
result = runner.invoke(cli, ['audit'])
print(result.output)
assert result.exit_code == 0, f'Exit code: {result.exit_code}, output: {result.output}'

# Verify audit_scores were written
conn = get_connection(p)
rows = conn.execute('SELECT * FROM audit_scores').fetchall()
assert len(rows) == 1, f'Expected 1 audit row, got {len(rows)}'
row = dict(rows[0])
assert row['letter_grade'] in ('A','B','C','D','F'), f'Bad grade: {row[\"letter_grade\"]}'
assert row['findings_json'] is not None
print(f'Audit score: {row[\"composite_score\"]:.2f} = {row[\"letter_grade\"]}')
conn.close()
print('ALL CHECKS PASSED')
"</automated>
    <manual>Run `mdc-encyclopedia audit` against a real database and verify Rich output displays correctly</manual>
  </verify>
  <done>Running `mdc-encyclopedia audit` iterates all datasets, computes quality scores, writes to audit_scores table, and displays a Rich-formatted summary with grade distribution.</done>
</task>

<task type="auto">
  <name>Task 2: Wire stats CLI command</name>
  <files>src/mdc_encyclopedia/cli.py</files>
  <action>
Replace the `stats()` stub command in cli.py with a full implementation.

1. Add `@click.pass_context` decorator to the stats function.

2. Implementation:
   - Get db_path from ctx.obj["db_path"], open connection
   - Check total datasets: `SELECT COUNT(*) FROM datasets`
   - If zero: print "[yellow]No datasets in database. Run `mdc-encyclopedia pull` first.[/yellow]" and return

3. **Datasets by Portal** Rich Table:
   - Query: `SELECT source_portal, COUNT(*) as cnt FROM datasets GROUP BY source_portal`
   - Table columns: Portal (bold), Count (right-aligned)
   - Add a Total row at the bottom

4. **Enrichment Status** Rich Table:
   - Enriched count: `SELECT COUNT(*) FROM enrichments`
   - Pending = total_datasets - enriched
   - Table columns: Status (bold), Count (right-aligned)
   - Color enriched green, pending yellow

5. **Quality Score Distribution** (conditional -- only if audit has been run):
   - Check: `SELECT COUNT(*) FROM audit_scores`
   - If > 0: Query `SELECT letter_grade, COUNT(*) as cnt FROM audit_scores GROUP BY letter_grade ORDER BY letter_grade`
   - Rich Table columns: Grade (bold), Count (right-aligned), Percentage (right-aligned)
   - Color grades: A=green, B=blue, C=yellow, D=red, F=bold red
   - If audit not run: show a Panel with "[yellow]No audit data. Run `mdc-encyclopedia audit` first.[/yellow]"

6. **Top Findings** (conditional -- only if audit has been run):
   - Query all findings_json from audit_scores, parse JSON, flatten, count occurrences
   - Show top 5 most common findings in a Rich Table with Finding, Count columns
   - If no findings to show, skip this section

7. Close connection.
  </action>
  <verify>
    <automated>cd /home/d48reu/DataDictionary && python -c "
import tempfile, os, json
from mdc_encyclopedia.db import init_db, get_connection, upsert_dataset, upsert_audit_score

# Create temp DB with test data
p = os.path.join(tempfile.mkdtemp(), 'test.db')
init_db(p)
conn = get_connection(p)
upsert_dataset(conn, {
    'id': 'test-1', 'source_portal': 'arcgis_hub', 'source_url': 'http://test',
    'title': 'Test Dataset', 'description': 'A test dataset description.',
    'category': 'Test', 'publisher': 'MDC', 'format': 'CSV',
    'created_at': '2025-01-01T00:00:00+00:00', 'updated_at': '2026-02-01T00:00:00+00:00',
    'row_count': 100, 'tags': '[\"test\"]', 'license': 'Public',
    'api_endpoint': None, 'bbox': None, 'download_url': None, 'metadata_json': None
})
upsert_audit_score(conn, 'test-1', 0.75, 'B', 0.8, 0.7, 0.5, json.dumps(['Brief description (26 chars)']))
conn.close()

# Run stats via CLI runner
os.environ['MDC_ENCYCLOPEDIA_DB'] = p
from click.testing import CliRunner
from mdc_encyclopedia.cli import cli
runner = CliRunner()
result = runner.invoke(cli, ['stats'])
print(result.output)
assert result.exit_code == 0, f'Exit code: {result.exit_code}, output: {result.output}'
assert 'arcgis_hub' in result.output or 'Portal' in result.output
assert 'Enrichment' in result.output or 'Pending' in result.output or 'pending' in result.output.lower()
assert 'B' in result.output  # Should show grade B
print('ALL CHECKS PASSED')
"</automated>
    <manual>Run `mdc-encyclopedia stats` and verify all three panels (portal counts, enrichment status, quality distribution) display correctly</manual>
  </verify>
  <done>Running `mdc-encyclopedia stats` displays Rich-formatted tables showing dataset counts per portal, enrichment status (enriched vs pending), and quality score distribution with letter grades. Works gracefully when audit has not been run yet.</done>
</task>

</tasks>

<verification>
1. `mdc-encyclopedia audit` on empty DB prints helpful message and exits cleanly
2. `mdc-encyclopedia audit` on populated DB writes scores to audit_scores and shows summary
3. `mdc-encyclopedia stats` before audit shows portal/enrichment data and "no audit data" message
4. `mdc-encyclopedia stats` after audit shows all three sections including grade distribution
5. Running audit twice does not create duplicate rows (INSERT OR REPLACE)
</verification>

<success_criteria>
- `mdc-encyclopedia audit` processes all datasets and writes letter grades + findings to SQLite
- `mdc-encyclopedia stats` displays a Rich dashboard with portal counts, enrichment status, and quality distribution
- Both commands handle empty database gracefully
- Re-running audit updates existing scores (no duplicates)
</success_criteria>

<output>
After completion, create `.planning/phases/04-quality-audit/04-02-SUMMARY.md`
</output>
