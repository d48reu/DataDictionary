---
phase: 07-deployment
plan: 02
type: execute
wave: 2
depends_on:
  - "07-01"
files_modified: []
autonomous: false
requirements:
  - DEPLOY-01

must_haves:
  truths:
    - "User has confirmed the workflow file is correct and ready for deployment"
    - "User knows the one-time GitHub Pages setup steps required before first deploy"
  artifacts: []
  key_links: []
---

<objective>
Verify the deployment workflow is ready and provide one-time setup instructions.

Purpose: The GitHub Pages deployment requires a one-time manual configuration in the repository settings that cannot be automated. This plan ensures the user knows what to do and can verify the workflow is correct.
Output: User confirmation that the workflow is ready and they understand the one-time setup.
</objective>

<execution_context>
@/home/d48reu/.claude/get-shit-done/workflows/execute-plan.md
@/home/d48reu/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-deployment/07-01-SUMMARY.md
@.planning/phases/07-deployment/07-RESEARCH.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Verify workflow and one-time GitHub Pages setup</name>
  <files>.github/workflows/encyclopedia.yml</files>
  <action>
CHECKPOINT: Present the user with the completed workflow file and one-time setup instructions.

What was built in Plan 01:
- Complete GitHub Actions workflow at `.github/workflows/encyclopedia.yml`
- Weekly cron (Monday 6 AM UTC), push-to-master (fast path), manual dispatch triggers
- Conditional enrichment with Haiku model, 25-dataset threshold, GitHub issue creation
- SQLite DB committed to repo for persistence with [skip ci]
- Artifact-based GitHub Pages deployment

Present the user with these one-time setup steps:

1. **GitHub Pages setup:** Repository Settings > Pages > Source > select "GitHub Actions"
2. **Optional secret:** Repository Settings > Secrets > Actions > add `ANTHROPIC_API_KEY`
3. **Trigger test:** Push to master or use Actions tab > "Encyclopedia Pipeline" > "Run workflow"
4. **Verify:** Check Actions tab for successful run, visit the Pages URL from deploy output

Wait for user to type "approved" or describe issues.
  </action>
  <verify>User confirms "approved" or provides feedback on issues to fix</verify>
  <done>User has reviewed the workflow, understands the one-time Pages and optional secret setup, and approves the deployment configuration</done>
</task>

</tasks>

<verification>
1. User has reviewed the workflow file
2. User understands the one-time Pages source configuration
3. User understands the optional ANTHROPIC_API_KEY secret setup
</verification>

<success_criteria>
- User confirms the workflow is ready for deployment
- User knows to set Pages source to "GitHub Actions" in repo settings
</success_criteria>

<output>
After completion, create `.planning/phases/07-deployment/07-02-SUMMARY.md`
</output>
