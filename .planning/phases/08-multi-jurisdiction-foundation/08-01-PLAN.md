---
phase: 08-multi-jurisdiction-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/mdc_encyclopedia/jurisdictions.yaml
  - src/mdc_encyclopedia/registry.py
  - src/mdc_encyclopedia/db.py
  - pyproject.toml
autonomous: true
requirements:
  - MULTI-01
  - MULTI-03

must_haves:
  truths:
    - "jurisdictions.yaml contains entries for miami-dade, broward, and miami with correct hub URLs"
    - "load_registry() returns all 3 jurisdictions with slug, display_name, hub_url, portal_type"
    - "Schema V3 migration creates synthetic composite PK, jurisdiction column, and ai_description column"
    - "All existing Miami-Dade datasets get 'miami-dade_' ID prefix and jurisdiction='miami-dade' after migration"
    - "Row counts for datasets, columns, enrichments, audit_scores, and changes match before and after migration"
    - "Auto-backup creates a timestamped copy of the database before any V3 migration SQL runs"
  artifacts:
    - path: "src/mdc_encyclopedia/jurisdictions.yaml"
      provides: "YAML registry with 3 South Florida jurisdictions"
      contains: "miami-dade"
    - path: "src/mdc_encyclopedia/registry.py"
      provides: "Registry loader with fallback chain"
      exports: ["load_registry"]
    - path: "src/mdc_encyclopedia/db.py"
      provides: "Schema V3 migration with synthetic composite key"
      contains: "_migrate_v2_to_v3"
    - path: "pyproject.toml"
      provides: "PyYAML dependency and jurisdictions.yaml force-include"
      contains: "pyyaml"
  key_links:
    - from: "src/mdc_encyclopedia/registry.py"
      to: "src/mdc_encyclopedia/jurisdictions.yaml"
      via: "Path(__file__).parent / jurisdictions.yaml"
      pattern: "Path\\(__file__\\)\\.parent"
    - from: "src/mdc_encyclopedia/db.py"
      to: "_migrate_v2_to_v3"
      via: "init_db auto-migration when user_version < 3"
      pattern: "user_version.*<.*3"
    - from: "src/mdc_encyclopedia/db.py"
      to: "_backup_database"
      via: "backup called before any V3 schema SQL"
      pattern: "_backup_database.*_migrate_v2_to_v3"
---

<objective>
Create the jurisdiction registry configuration and Schema V3 database migration that form the foundation for multi-jurisdiction support.

Purpose: Every other Phase 8 change depends on the registry existing (to look up portal URLs) and the schema supporting jurisdiction-prefixed composite keys (to store multi-jurisdiction data without collisions).

Output: jurisdictions.yaml config file, registry.py loader module, Schema V3 migration in db.py, PyYAML dependency in pyproject.toml.
</objective>

<execution_context>
@/home/d48reu/.claude/get-shit-done/workflows/execute-plan.md
@/home/d48reu/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-multi-jurisdiction-foundation/08-CONTEXT.md
@.planning/phases/08-multi-jurisdiction-foundation/08-RESEARCH.md
@src/mdc_encyclopedia/db.py
@pyproject.toml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create jurisdiction registry config and loader module</name>
  <files>
    src/mdc_encyclopedia/jurisdictions.yaml
    src/mdc_encyclopedia/registry.py
    pyproject.toml
  </files>
  <action>
    1. Create `src/mdc_encyclopedia/jurisdictions.yaml` with the three jurisdictions per user decision:
       ```yaml
       jurisdictions:
         miami-dade:
           display_name: "Miami-Dade County"
           hub_url: "https://opendata.miamidade.gov"
           portal_type: "arcgis_hub"
         broward:
           display_name: "Broward County"
           hub_url: "https://geohub-bcgis.opendata.arcgis.com"
           portal_type: "arcgis_hub"
         miami:
           display_name: "City of Miami"
           hub_url: "https://datahub-miamigis.opendata.arcgis.com"
           portal_type: "arcgis_hub"
       ```

    2. Create `src/mdc_encyclopedia/registry.py` with a `load_registry()` function.
       - Fallback chain: explicit path arg > `MDC_ENCYCLOPEDIA_JURISDICTIONS` env var > `~/.local/share/mdc-encyclopedia/jurisdictions.yaml` > package default (`Path(__file__).parent / "jurisdictions.yaml"`).
       - Use `yaml.safe_load()` from PyYAML.
       - Return the `jurisdictions` dict from the YAML. If the key is missing or file is empty, raise a clear ValueError.
       - Add a `get_jurisdiction(slug)` convenience function that returns one jurisdiction's config or raises KeyError with available slugs listed.

    3. Add `"pyyaml>=6.0"` to the dependencies list in `pyproject.toml`.

    4. Add a force-include line for `jurisdictions.yaml` in `pyproject.toml` so it ships in the wheel:
       ```toml
       "src/mdc_encyclopedia/jurisdictions.yaml" = "mdc_encyclopedia/jurisdictions.yaml"
       ```
  </action>
  <verify>
    <automated>cd /home/d48reu/DataDictionary && python -c "from mdc_encyclopedia.registry import load_registry; r = load_registry(); assert len(r) == 3; assert 'miami-dade' in r; assert 'broward' in r; assert 'miami' in r; assert r['broward']['hub_url'] == 'https://geohub-bcgis.opendata.arcgis.com'; print('Registry OK:', list(r.keys()))"</automated>
    <manual>Verify jurisdictions.yaml is readable YAML and pyproject.toml has pyyaml in dependencies</manual>
  </verify>
  <done>load_registry() returns 3 jurisdictions with correct slugs, display names, hub URLs, and portal types. PyYAML is a project dependency. YAML file is force-included for wheel distribution.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Schema V3 migration with synthetic composite key and auto-backup</name>
  <files>src/mdc_encyclopedia/db.py</files>
  <action>
    1. Add a `_backup_database(db_path: str) -> str` function:
       - Uses `shutil.copy2()` to create `{db_path}.backup-{YYYYMMDDTHHMMSS}`.
       - Returns the backup path.
       - Prints a message to stdout about the backup location.

    2. Add a `_migrate_v2_to_v3(conn, db_path: str)` function implementing the create-copy-swap migration from research:
       - Count rows in datasets, columns, enrichments, audit_scores, changes BEFORE migration.
       - `PRAGMA foreign_keys=OFF`
       - Create `datasets_v3` table with: id TEXT PRIMARY KEY, jurisdiction TEXT NOT NULL, arcgis_id TEXT NOT NULL, source_portal TEXT NOT NULL, source_url TEXT, title TEXT, description TEXT, ai_description TEXT, category TEXT, publisher TEXT, format TEXT, created_at TEXT, updated_at TEXT, row_count INTEGER, tags TEXT, license TEXT, api_endpoint TEXT, bbox TEXT, download_url TEXT, metadata_json TEXT, pulled_at TEXT NOT NULL DEFAULT (datetime('now')), UNIQUE(jurisdiction, arcgis_id).
       - INSERT INTO datasets_v3 from datasets: prefix id with `'miami-dade_' || id`, set jurisdiction='miami-dade', set arcgis_id=id (the original), copy all other columns, set ai_description from enrichments JOIN.
       - UPDATE columns SET dataset_id = 'miami-dade_' || dataset_id
       - UPDATE enrichments SET dataset_id = 'miami-dade_' || dataset_id
       - UPDATE audit_scores SET dataset_id = 'miami-dade_' || dataset_id
       - UPDATE changes SET dataset_id = 'miami-dade_' || dataset_id
       - Populate ai_description via UPDATE datasets_v3 SET ai_description = (SELECT description FROM enrichments WHERE enrichments.dataset_id = datasets_v3.id)
       - DROP TABLE datasets; ALTER TABLE datasets_v3 RENAME TO datasets
       - CREATE INDEX idx_datasets_jurisdiction ON datasets(jurisdiction)
       - CREATE INDEX idx_datasets_source_portal ON datasets(source_portal)
       - `PRAGMA foreign_keys=ON`
       - `PRAGMA foreign_key_check` — if results, raise error
       - Count rows AFTER migration. Assert all counts match. Log counts.
       - Set `PRAGMA user_version=3`

    3. Modify `init_db()`:
       - After the existing V1->V2 migration check, add a V2->V3 migration check.
       - If `user_version < 3` AND the database has existing data (datasets table exists with rows), call `_backup_database(db_path)` FIRST, then `_migrate_v2_to_v3(conn, db_path)`.
       - If `user_version < 3` AND database is fresh (no datasets table or 0 rows), create the V3 schema directly (the CREATE TABLE should be the V3 version with jurisdiction, arcgis_id, ai_description columns).
       - Update the initial CREATE TABLE statement to be the V3 schema so new databases start at V3.

    4. Add a `get_datasets_by_jurisdiction(conn)` helper that returns `SELECT jurisdiction, COUNT(*) as cnt FROM datasets GROUP BY jurisdiction ORDER BY jurisdiction`.

    CRITICAL: The backup MUST happen BEFORE any migration SQL. If the migration fails partway through, the user can restore from the backup file.

    CRITICAL: All 4 child tables (columns, enrichments, audit_scores, changes) must have their dataset_id FK updated. Missing even one causes FK violations.
  </action>
  <verify>
    <automated>cd /home/d48reu/DataDictionary && python -c "
import sqlite3, tempfile, os
# Create a V2 test database
db_path = os.path.join(tempfile.mkdtemp(), 'test.db')
conn = sqlite3.connect(db_path)
conn.execute('PRAGMA user_version=2')
conn.execute('CREATE TABLE datasets (id TEXT PRIMARY KEY, source_portal TEXT, source_url TEXT, title TEXT, description TEXT, category TEXT, publisher TEXT, format TEXT, created_at TEXT, updated_at TEXT, row_count INTEGER, tags TEXT, license TEXT, api_endpoint TEXT, bbox TEXT, download_url TEXT, metadata_json TEXT, pulled_at TEXT NOT NULL DEFAULT (datetime(\"now\")))')
conn.execute('CREATE TABLE columns (id INTEGER PRIMARY KEY, dataset_id TEXT REFERENCES datasets(id), name TEXT, data_type TEXT, description TEXT)')
conn.execute('CREATE TABLE enrichments (id INTEGER PRIMARY KEY, dataset_id TEXT UNIQUE REFERENCES datasets(id), description TEXT, category TEXT, department TEXT, update_frequency TEXT, use_cases TEXT, accessibility_notes TEXT, prompt_version TEXT, model TEXT, enriched_at TEXT)')
conn.execute('CREATE TABLE audit_scores (id INTEGER PRIMARY KEY, dataset_id TEXT UNIQUE REFERENCES datasets(id), freshness_score REAL, completeness_score REAL, documentation_score REAL, composite_score REAL, letter_grade TEXT, findings_json TEXT, audited_at TEXT)')
conn.execute('CREATE TABLE changes (id INTEGER PRIMARY KEY, dataset_id TEXT, change_type TEXT, details TEXT, detected_at TEXT)')
conn.execute(\"INSERT INTO datasets (id, source_portal, title, pulled_at) VALUES ('abc123', 'arcgis_hub', 'Test Dataset', datetime('now'))\")
conn.execute(\"INSERT INTO columns (dataset_id, name, data_type) VALUES ('abc123', 'col1', 'text')\")
conn.execute(\"INSERT INTO enrichments (dataset_id, description, prompt_version) VALUES ('abc123', 'Test enrichment', '1.0')\")
conn.execute(\"INSERT INTO audit_scores (dataset_id, freshness_score, composite_score, letter_grade) VALUES ('abc123', 0.8, 0.75, 'B')\")
conn.execute(\"INSERT INTO changes (dataset_id, change_type, detected_at) VALUES ('abc123', 'added', datetime('now'))\")
conn.commit()
conn.close()
# Now run init_db which should trigger V3 migration
from mdc_encyclopedia.db import init_db, get_connection
init_db(db_path)
conn = get_connection(db_path)
# Verify migration
row = conn.execute('SELECT id, jurisdiction, arcgis_id FROM datasets').fetchone()
assert row['id'] == 'miami-dade_abc123', f'Expected miami-dade_abc123, got {row[\"id\"]}'
assert row['jurisdiction'] == 'miami-dade'
assert row['arcgis_id'] == 'abc123'
# Verify FK updates
col = conn.execute('SELECT dataset_id FROM columns').fetchone()
assert col['dataset_id'] == 'miami-dade_abc123'
enr = conn.execute('SELECT dataset_id FROM enrichments').fetchone()
assert enr['dataset_id'] == 'miami-dade_abc123'
aud = conn.execute('SELECT dataset_id FROM audit_scores').fetchone()
assert aud['dataset_id'] == 'miami-dade_abc123'
chg = conn.execute('SELECT dataset_id FROM changes').fetchone()
assert chg['dataset_id'] == 'miami-dade_abc123'
# Verify ai_description populated
ds = conn.execute('SELECT ai_description FROM datasets').fetchone()
assert ds['ai_description'] == 'Test enrichment'
# Verify backup exists
import glob
backups = glob.glob(db_path + '.backup-*')
assert len(backups) == 1, f'Expected 1 backup, found {len(backups)}'
ver = conn.execute('PRAGMA user_version').fetchone()[0]
assert ver == 3
print('V3 migration OK: synthetic key, FK updates, ai_description, backup all verified')
conn.close()
"</automated>
  </verify>
  <done>Schema V3 migration works end-to-end: auto-backup, synthetic composite key, jurisdiction column, ai_description denormalization, all 4 child table FK updates, row count verification, auto-migrate on init_db, fresh databases start at V3.</done>
</task>

</tasks>

<verification>
1. `python -c "from mdc_encyclopedia.registry import load_registry; print(load_registry())"` returns 3 jurisdictions
2. V3 migration test (in Task 2 verify) passes with synthetic key, FK updates, backup, and row count matching
3. `grep pyyaml pyproject.toml` shows the dependency
4. `grep jurisdictions.yaml pyproject.toml` shows the force-include
</verification>

<success_criteria>
- Registry module loads 3 jurisdictions from YAML config with fallback chain
- V3 migration handles existing V2 databases: backup, composite key, FK updates, ai_description, zero data loss
- Fresh databases created at V3 schema directly
- PyYAML dependency and YAML config force-included in wheel
</success_criteria>

<output>
After completion, create `.planning/phases/08-multi-jurisdiction-foundation/08-01-SUMMARY.md`
</output>
