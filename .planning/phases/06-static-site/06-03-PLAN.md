---
phase: 06-static-site
plan: 03
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - src/mdc_encyclopedia/site/templates/dataset.html
  - src/mdc_encyclopedia/site/templates/browse.html
  - src/mdc_encyclopedia/site/static/filter.js
  - src/mdc_encyclopedia/site/generator.py
autonomous: true
requirements:
  - SITE-02
  - SITE-03
  - SITE-04
  - SITE-05
  - SITE-06

must_haves:
  truths:
    - "Each dataset has its own page at /dataset/{slug}/ showing metadata, AI enrichment, columns, quality score, and source link"
    - "Related datasets are shown on each dataset page based on tag/category overlap"
    - "Freshness indicators (relative time + colored dot) appear on listings and detail pages"
    - "Category browse pages list datasets with compact rows (title, 1-line description, department chip, quality dot)"
    - "Browse pages support client-side filtering by format, publisher, and tag"
  artifacts:
    - path: "src/mdc_encyclopedia/site/templates/dataset.html"
      provides: "Individual dataset detail page template"
      min_lines: 80
      contains: "related"
    - path: "src/mdc_encyclopedia/site/templates/browse.html"
      provides: "Category browse page with filterable dataset list"
      min_lines: 60
      contains: "dataset-row"
    - path: "src/mdc_encyclopedia/site/static/filter.js"
      provides: "Client-side filtering logic for browse pages"
      min_lines: 30
  key_links:
    - from: "src/mdc_encyclopedia/site/templates/dataset.html"
      to: "src/mdc_encyclopedia/site/context.py"
      via: "template receives dataset dict with related_datasets list from context"
      pattern: "related_datasets"
    - from: "src/mdc_encyclopedia/site/templates/browse.html"
      to: "src/mdc_encyclopedia/site/static/filter.js"
      via: "browse page rows have data attributes consumed by filter JS"
      pattern: "data-format.*data-publisher"
    - from: "src/mdc_encyclopedia/site/generator.py"
      to: "src/mdc_encyclopedia/site/templates/dataset.html"
      via: "generator renders one dataset page per dataset in the catalog"
      pattern: "render_dataset_pages"
---

<objective>
Build dataset detail pages and category browse pages with filtering, freshness indicators, and related datasets.

Purpose: These are the core content pages of the encyclopedia. Dataset pages are where residents learn about individual datasets. Browse pages enable discovery by category with dense, scannable listings.

Output: Individual dataset pages with full metadata display, category browse pages with client-side filtering, freshness indicators, and related dataset links.
</objective>

<execution_context>
@/home/d48reu/.claude/get-shit-done/workflows/execute-plan.md
@/home/d48reu/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-static-site/06-RESEARCH.md
@.planning/phases/06-static-site/06-01-SUMMARY.md
@src/mdc_encyclopedia/site/generator.py
@src/mdc_encyclopedia/site/context.py
@src/mdc_encyclopedia/site/templates/base.html
@src/mdc_encyclopedia/site/static/style.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Dataset detail page template with enrichment, columns, quality, and related datasets</name>
  <files>
    src/mdc_encyclopedia/site/templates/dataset.html
    src/mdc_encyclopedia/site/generator.py
  </files>
  <action>
**templates/dataset.html**: Replace stub with full dataset detail page extending base.html.

Page section order (per research discretion recommendation):
1. **Header**: Dataset title (h1) with letter grade badge inline. Breadcrumbs: Home > Browse > {Category} > {Title}.

2. **AI Description**: Rich paragraph with the Claude-generated plain-English description. If no enrichment, show a muted "This dataset has not yet been AI-described" message.

3. **Use Cases**: "Why This Matters" section. Bulleted list of use cases from enrichment. Each use case is a practical scenario a resident or journalist would care about. Guard with `{% if dataset.use_cases %}`.

4. **Key Metadata table**: Two-column table with:
   - Publisher: `{{ dataset.publisher }}`
   - Department: `{{ dataset.department }}` (from enrichment, with chip styling)
   - Category: `{{ dataset.category }}` (link to browse page)
   - Format: `{{ dataset.format }}`
   - Last Updated: `{{ dataset.relative_time }}` with freshness dot (colored span based on staleness_color)
   - Rows: `{{ dataset.row_count|default('N/A') }}`
   - Civic Relevance: `{{ dataset.civic_relevance }}` (HIGH/MEDIUM/LOW with color)
   - Update Frequency: `{{ dataset.update_freq|default('Unknown') }}`
   - Source: Link to `{{ dataset.source_url }}` (opens in new tab)
   - Keywords: list of keyword chips from enrichment

5. **Columns/Fields table**: If columns exist, show a table with Name, Type, Description. Use `{% for col in dataset.columns %}`. If no columns, show "Column metadata not available" muted text.

6. **Related Datasets**: "Related Datasets" section. List of up to 5 related datasets, each as a compact row with title (link to /dataset/{slug}/), department chip, and grade badge. Computed by context.py's `_build_related_datasets`. If none, show "No related datasets found."

7. **Quality Breakdown**: Section showing individual audit dimension scores (freshness, completeness, documentation) as progress bars or simple labels. Show overall composite score and letter grade. Show findings list if any exist. Guard entire section with `{% if dataset.audit %}`.

**generator.py**: Update `_render_dataset_pages` to iterate over all datasets in site_data, render dataset.html for each, writing to `{output_dir}/dataset/{slug}/index.html`. Pass the full dataset dict (which includes columns, related_datasets, audit data) as template context. Ensure related_datasets are computed for each dataset via context.py before rendering.
  </action>
  <verify>
    <automated>cd /home/d48reu/DataDictionary && python -c "
from jinja2 import Environment, FileSystemLoader, select_autoescape
import os
tmpl_dir = 'src/mdc_encyclopedia/site/templates'
env = Environment(loader=FileSystemLoader(tmpl_dir), autoescape=select_autoescape(['html']))
t = env.get_template('dataset.html')
ds = {'title': 'Test Dataset', 'slug': 'test-dataset', 'description': 'A test', 'ai_description': 'AI desc', 'use_cases': ['Use 1'], 'keywords': ['key1'], 'publisher': 'ITD', 'department': 'IT', 'category': 'Technology', 'category_slug': 'technology', 'format': 'GeoJSON', 'relative_time': '3 days ago', 'staleness_color': 'green', 'row_count': 100, 'civic_relevance': 'HIGH', 'update_freq': 'Weekly', 'source_url': 'https://example.com', 'grade': 'A', 'grade_class': 'grade-a', 'columns': [{'name': 'id', 'data_type': 'integer', 'description': 'Primary key'}], 'related_datasets': [{'title': 'Related', 'slug': 'related', 'department': 'IT', 'grade': 'B', 'grade_class': 'grade-b'}], 'audit': {'freshness_score': 0.9, 'completeness_score': 0.8, 'documentation_score': 0.7, 'composite_score': 0.82, 'letter_grade': 'A', 'findings': ['Good metadata']}}
html = t.render(dataset=ds, generated_at='2026-02-26')
assert 'Test Dataset' in html
assert 'AI desc' in html
assert 'Related' in html
print('Dataset template renders correctly')
"</automated>
  </verify>
  <done>Dataset detail page renders all sections: AI description, use cases, metadata table, columns, related datasets, and quality breakdown. Freshness indicators show relative time with colored dots. Letter grade badge displayed prominently. Missing data handled gracefully.</done>
</task>

<task type="auto">
  <name>Task 2: Category browse pages with compact list rows and client-side filtering</name>
  <files>
    src/mdc_encyclopedia/site/templates/browse.html
    src/mdc_encyclopedia/site/static/filter.js
    src/mdc_encyclopedia/site/generator.py
  </files>
  <action>
**templates/browse.html**: Replace stub with full category browse page extending base.html.

Layout (per locked decisions — compact list rows, dense and scannable):
1. **Page header**: "{Category Name}" (h1) with dataset count. If rendering the "all datasets" browse page (no category filter), title is "Browse All Datasets" with total count.

2. **Filter bar**: Inline bar above the dataset list with:
   - Format dropdown: populated from unique formats in this category's datasets. Default "All Formats".
   - Publisher dropdown: populated from unique publishers. Default "All Publishers".
   - Tag dropdown (or multi-select): populated from unique tags. Default "All Tags".
   - Active filter chips: shown below dropdowns, removable with X.
   - Result count: "{N} datasets" updates live as filters change.

3. **Dataset list**: Compact rows (NOT cards per locked decision). Each row is a `<div class="dataset-row">` (or `<tr>`) with data attributes for filtering:
   - `data-format="{{ ds.format|lower }}"`
   - `data-publisher="{{ ds.publisher|lower }}"`
   - `data-tags="{{ ds.tags_text|lower }}"` (comma-separated)

   Row content:
   - Title: link to /dataset/{slug}/ (bold, primary text)
   - Description: 1-line AI description snippet (muted, truncated with CSS text-overflow)
   - Department chip: small colored chip
   - Quality grade: letter grade badge
   - Freshness: colored dot + relative time (compact)

4. **Empty state**: If category has no datasets, show "No datasets in this category" message.

**static/filter.js**: Client-side filtering logic (separate from search.js):
- On DOMContentLoaded, populate dropdown options from data attributes in the page's dataset rows.
- `applyFilters()` — reads selected values from each dropdown. For each `.dataset-row`, check data attributes against active filters. Toggle `display: none` for non-matching rows. Update the result count text.
- Event listeners on dropdown change events calling `applyFilters()`.
- "Clear all" button that resets all dropdowns and shows all rows.
- Active filter chips: when a filter is selected, show a chip with "Format: GeoJSON [x]". Clicking X clears that filter.

**generator.py**: Update `_render_browse_pages`:
- Render one browse page per category at `{output_dir}/browse/{category-slug}/index.html`. Pass category name, dataset list for that category, unique formats/publishers/tags for filter dropdowns.
- Render an "all datasets" browse page at `{output_dir}/browse/index.html` with all datasets.
- Ensure category slugs are generated via python-slugify. Handle None/empty categories as "Uncategorized".
- Include filter.js in browse pages (add script tag in browse.html extending base scripts block).
  </action>
  <verify>
    <automated>cd /home/d48reu/DataDictionary && python -c "
from jinja2 import Environment, FileSystemLoader, select_autoescape
tmpl_dir = 'src/mdc_encyclopedia/site/templates'
env = Environment(loader=FileSystemLoader(tmpl_dir), autoescape=select_autoescape(['html']))
t = env.get_template('browse.html')
datasets = [{'title': 'Dataset 1', 'slug': 'dataset-1', 'ai_description': 'Test description for dataset one', 'department': 'ITD', 'grade': 'A', 'grade_class': 'grade-a', 'format': 'GeoJSON', 'publisher': 'ITD', 'tags_text': 'gis,roads', 'relative_time': '2 days ago', 'staleness_color': 'green'}]
html = t.render(category_name='Transportation', datasets=datasets, dataset_count=1, formats=['geojson'], publishers=['itd'], tags=['gis','roads'], generated_at='2026-02-26')
assert 'Transportation' in html
assert 'dataset-row' in html
assert 'data-format' in html
print('Browse template renders correctly')
"</automated>
  </verify>
  <done>Category browse pages render with compact list rows, each showing title, description, department chip, grade badge, and freshness. Client-side filtering by format, publisher, and tag works via data attributes and vanilla JS. All-datasets browse page and per-category pages generated. Filter state reflected with removable chips and live result count.</done>
</task>

</tasks>

<verification>
1. Running export generates /dataset/{slug}/index.html for every dataset in the DB
2. Dataset pages show AI description, use cases, metadata, columns, related datasets, and quality
3. Freshness indicators show relative time with green/yellow/red dots
4. Running export generates /browse/index.html and /browse/{category-slug}/index.html for each category
5. Browse page rows have data-format, data-publisher, data-tags attributes
6. filter.js filters rows based on dropdown selections without page reload
7. Related datasets section shows up to 5 datasets with links
8. Empty/missing data states handled gracefully (no template crashes)
</verification>

<success_criteria>
- Every dataset has a detail page with complete metadata display
- Related datasets shown based on tag/category overlap (up to 5)
- Freshness indicators (relative time + colored dot) on listings and detail pages
- Category browse pages with compact, scannable list rows
- Client-side filtering by format, publisher, and tag on browse pages
- Letter grade badges on both listing rows and detail pages
</success_criteria>

<output>
After completion, create `.planning/phases/06-static-site/06-03-SUMMARY.md`
</output>
